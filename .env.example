# This is an example .env file
# Create a copy named .env and replace the placeholder values with your actual credentials

# MCP.run Configuration
# Replace this with your signed URL from mcp.run (click "Generate URL" in the Connect Your Client modal)
MCP_SIGNED_URL=your_signed_url_from_mcp_run_here

# Server Configuration
PORT=3000
